name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v3

    - name: cppcheck
      run: |
        CXX=g++ bash scripts/build.sh --generator Ninja --config --cppcheck
